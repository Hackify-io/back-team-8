window.addEventListener('load', () =>{
    
/*
    let typeEvent = 'ontouchstart' in window ? 'touchstart' : 'click';

    charts_buttons = [document.getElementById("btn-1"), document.getElementById("btn-2"),
                      document.getElementById("btn-3")];

    for(let i = 0; i <= charts_buttons.lenght; i++)
        charts_buttons[i].addEventListener(typeEvent, () => {
            if(i == 0)
        });
*/

});